# Botaqi (.env.example) — DO NOT COMMIT REAL SECRETS
# Copy to .env.local for local dev OR set values in Vercel (server secrets only in Vercel)

# -----------------------
# Client / Public (safe to be client-visible)
# -----------------------
# Supabase public (client-side auth)
# Required by `src/supabase/client.ts` (Vite client):
# - `VITE_SUPABASE_URL` is used by the browser Supabase client (import.meta.env)
# - `VITE_SUPABASE_ANON_KEY` is the public anon key safe for client use
VITE_SUPABASE_URL=REPLACE_WITH_SUPABASE_URL
VITE_SUPABASE_ANON_KEY=REPLACE_WITH_SUPABASE_ANON_KEY

# Firebase (legacy, optional)
NEXT_PUBLIC_FIREBASE_API_KEY=REPLACE_WITH_CLIENT_API_KEY
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=REPLACE_WITH_PROJECT.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=botaqiy-arabic-learning
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=REPLACE_WITH_BUCKET.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=REPLACE_WITH_ID
NEXT_PUBLIC_FIREBASE_APP_ID=REPLACE_WITH_APP_ID
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=REPLACE_WITH_MEASUREMENT_ID

# Feature flags (client-visible)
NEXT_PUBLIC_ENABLE_ANALYTICS=true
NEXT_PUBLIC_FEATURE_RTL=true

# -----------------------
# Server / Secret (SET THESE IN VERCEL OR OTHER SECRET MANAGER)
# -----------------------
# Supabase (server-side / secret)
# Required server-side envs (used by `createServerClient()` and server code):
# - `SUPABASE_URL` (same base URL as VITE_SUPABASE_URL)
# - `SUPABASE_SERVICE_ROLE_KEY` (service role; keep secret)
# Optional/fallback keys referenced in some scripts/code:
# - `SUPABASE_KEY` (alias/fallback for service key if present)
# - `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` (older/public naming used in some archived scripts)
SUPABASE_URL=REPLACE_WITH_SUPABASE_URL
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_SUPABASE_SERVICE_ROLE_KEY
# Optional / fallback (only if you need them)
# SUPABASE_KEY=REPLACE_WITH_SUPABASE_KEY
# NEXT_PUBLIC_SUPABASE_URL=REPLACE_WITH_SUPABASE_URL
# NEXT_PUBLIC_SUPABASE_ANON_KEY=REPLACE_WITH_SUPABASE_ANON_KEY

# Database connection (used by some server-side tooling / migrations)
DATABASE_URL=postgres://user:pass@host:5432/dbname

# Supabase JWT secret (optional; used for custom JWT signing/RLS)
SUPABASE_JWT_SECRET=your-jwt-secret

# Firebase (server-side, if needed)
FIREBASE_ADMIN_KEY=REPLACE_WITH_BASE64_SERVICE_ACCOUNT_JSON

# OpenAI (server-side only)
OPENAI_API_KEY=REPLACE_WITH_OPENAI_KEY

# App-specific server API key used by internal endpoints (e.g., logBlocked)
LOGBLOCKED_API_KEY=REPLACE_WITH_SECURE_TOKEN

# Vercel: token for scripted API usage (store in Vercel/CI only)
VERCEL_TOKEN=REPLACE_WITH_VERCEL_TOKEN
VERCEL_PROJECT_ID=REPLACE_WITH_VERCEL_PROJECT_ID

# Sentry: DSN for client + server error tracking
SENTRY_DSN=REPLACE_WITH_SENTRY_DSN
SENTRY_AUTH_TOKEN=REPLACE_WITH_SENTRY_AUTH_TOKEN
SENTRY_ORG=REPLACE_WITH_ORG_NAME
SENTRY_PROJECT=botaqi-web

# -----------------------
# Local-only & Emulators (optional; keep out of prod)
# -----------------------
# Uncomment for local Supabase emulator usage during development
# SUPABASE_URL=http://localhost:54321
# SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...local_token

# Uncomment for local Firestore emulator usage during development
# FIRESTORE_EMULATOR_HOST=localhost:8081

# -----------------------
# NOTES
# -----------------------
# - Never commit a real .env file. Keep this example in repo.
# - Put all server secrets (SUPABASE_SERVICE_ROLE_KEY, FIREBASE_ADMIN_KEY, OPENAI_API_KEY, 
#   LOGBLOCKED_API_KEY, VERCEL_TOKEN, SENTRY_DSN) into Vercel (or other secret manager) 
#   before deployment.
# - For Supabase setup instructions, see docs/SUPABASE_SETUP.md
# - Ensure FIREBASE_ADMIN_KEY is base64(serviceAccountJson) if deploy scripts expect base64.
# - See botaqi-web/DEPLOYMENT_CHECKLIST.md for exact formats and validation commands.